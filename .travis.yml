language: ruby
before_install:
  - gem update --system
  - gem --version
install:
  - gem install bundler:2.3.8
script:
  - bundle install
  - bundle exec rake db:migrate RAILS_ENV=test
services:
  - redis-server

